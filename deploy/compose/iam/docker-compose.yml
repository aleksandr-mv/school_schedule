services:
  iam:
    build:
      context: ../../..
      dockerfile: deploy/compose/iam/Dockerfile
    container_name: iam-service
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    healthcheck:
      test: ["CMD", "grpc-health-probe", "-addr=iam:50051"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    restart: unless-stopped
    depends_on:
      postgres-iam-primary:
        condition: service_healthy
    networks:
      - school_net

  postgres-iam-primary:
    image: postgres:17.5-alpine
    container_name: iam-postgres-primary
    env_file:
      - .env
    volumes:
      - postgres_iam_primary_data:/var/lib/postgresql/data
      - ./postgresql.conf:/etc/postgresql/postgresql.conf:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d iam" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
        reservations:
          memory: 256M
          cpus: '0.25'
    restart: unless-stopped
    networks:
      - school_net

# Реплики отключены для упрощения разработки
  # Будут добавлены в Kubernetes через PostgreSQL Operator

volumes:
  postgres_iam_primary_data:

networks:
  school_net:
    external: true
