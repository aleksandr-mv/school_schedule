version: '3'

# =============================================================================
# ГЛОБАЛЬНЫЕ ПЕРЕМЕННЫЕ ПРОЕКТА
# =============================================================================

vars:
  # Версии инструментов разработки
  GO_VERSION: '1.24'
  GOLANGCI_LINT_VERSION: 'v2.2.1'
  GCI_VERSION: 'v0.13.6'
  GOFUMPT_VERSION: 'v0.8.0'
  BUF_VERSION: '1.55.1'
  PROTOC_GEN_GO_VERSION: 'v1.36.6'
  PROTOC_GEN_GO_GRPC_VERSION: 'v1.5.1'
  PROTOC_GEN_GRPC_GATEWAY_VERSION: 'v2.27.1'
  PROTOC_GEN_VALIDATE_VERSION: 'v1.2.1'
  PROTOC_GEN_OPENAPIV2_VERSION: 'v2.27.1'
  YQ_VERSION: 'v4.45.4'
  GRPCURL_VERSION: 'v1.9.3'
  MOCKERY_VERSION: 'v2.53.3'

  # Пути к инструментам
  BIN_DIR: '{{.ROOT_DIR}}/bin'
  GOLANGCI_LINT: '{{.BIN_DIR}}/golangci-lint'
  GCI: '{{.BIN_DIR}}/gci'
  GOFUMPT: '{{.BIN_DIR}}/gofumpt'
  BUF: '{{.BIN_DIR}}/buf'
  YQ: '{{.BIN_DIR}}/yq'
  PROTOC_GEN_GO: '{{.BIN_DIR}}/protoc-gen-go'
  PROTOC_GEN_GO_GRPC: '{{.BIN_DIR}}/protoc-gen-go-grpc'
  PROTOC_GEN_GRPC_GATEWAY: '{{.BIN_DIR}}/protoc-gen-grpc-gateway'
  PROTOC_GEN_VALIDATE: '{{.BIN_DIR}}/protoc-gen-validate'
  PROTOC_GEN_OPENAPIV2: '{{.BIN_DIR}}/protoc-gen-openapiv2'
  GRPCURL: '{{.BIN_DIR}}/grpcurl'
  MOCKERY: "{{.BIN_DIR}}/mockery"

  # Пути к файлам проекта
  COVERAGE_DIR: '{{.ROOT_DIR}}/coverage'
  COVERAGE_FILE: total.out

  # Модули и сервисы
  MODULES: platform shared iam rbac
  SERVICES: cache,eventbus,envoy,iam,logs,metrics,rbac,tracing
  GRPC_SERVER_ADDR: 'localhost:50051'

# =============================================================================
# УСТАНОВКА ИНСТРУМЕНТОВ РАЗРАБОТКИ
# =============================================================================

tasks:
  # Форматирование кода
  install-formatters:
    desc: "Устанавливает форматтеры gci и gofumpt в ./bin"
    summary: |
      Эта задача проверяет наличие инструментов форматирования кода gofumpt и gci в директории bin.
      Если инструменты не найдены, они будут автоматически установлены с указанными версиями.
      
      Используется:
        - gofumpt: для форматирования кода Go
        - gci: для сортировки импортов Go
    cmds:
      - |
        [ -f {{.GOFUMPT}} ] || {
          echo '📦 Устанавливаем gofumpt {{.GOFUMPT_VERSION}}...'
          GOBIN={{.BIN_DIR}} go install mvdan.cc/gofumpt@{{.GOFUMPT_VERSION}}
        }
        [ -f {{.GCI}} ] || {
          echo '📦 Устанавливаем gci {{.GCI_VERSION}}...'
          GOBIN={{.BIN_DIR}} go install github.com/daixiang0/gci@{{.GCI_VERSION}}
        }
    status:
      - test -x {{.GOFUMPT}}
      - test -x {{.GCI}}

  install-golangci-lint:
    desc: "Устанавливает golangci-lint в каталог bin"
    summary: |
      Проверяет наличие golangci-lint в директории bin.
      Если инструмент не найден, автоматически устанавливает его через go install.
      
      Устанавливаемая версия: {{.GOLANGCI_LINT_VERSION}}
    cmds:
      - |
        [ -f {{.GOLANGCI_LINT}} ] || {
          mkdir -p {{.BIN_DIR}}
          echo "📦 Устанавливаем golangci-lint {{.GOLANGCI_LINT_VERSION}}..."
          GOBIN={{.BIN_DIR}} go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@{{.GOLANGCI_LINT_VERSION}}
        }
    status:
      - test -x {{.GOLANGCI_LINT}}

  install-buf:
    desc: "Устанавливает Buf в каталог bin"
    cmds:
      - |
        [ -f {{.BUF}} ] || {
          mkdir -p {{.BIN_DIR}} tmp-buf
          curl -sSL \
            "https://github.com/bufbuild/buf/releases/download/v{{.BUF_VERSION}}/buf-$(uname -s)-$(uname -m).tar.gz" \
            | tar -xz -C tmp-buf
          mv tmp-buf/buf/bin/buf {{.BUF}}
          rm -rf tmp-buf
          chmod +x {{.BUF}}
        }

  yq:install:
    desc: "Устанавливает yq в bin/ при необходимости"
    cmds:
      - |
        [ -f {{.YQ}} ] || {
          echo '📦 Installing yq...'
          GOBIN={{.BIN_DIR}} go install github.com/mikefarah/yq/v4@{{.YQ_VERSION}}
        }

  mockery:install:
    desc: "Устанавливает mockery в ./bin"
    cmds:
      - |
        [ -f {{.MOCKERY}} ] || {
          echo '📦 Installing mockery...'
          GOBIN={{.BIN_DIR}} go install github.com/vektra/mockery/v2@{{.MOCKERY_VERSION}}
        }
    status:
      - test -x {{.MOCKERY}}

  grpcurl:install:
    desc: "Устанавливает grpcurl в каталог bin"
    cmds:
      - |
        [ -f {{.GRPCURL}} ] || {
          echo '📦 Устанавливаем grpcurl {{.GRPCURL_VERSION}}...'
          GOBIN={{.BIN_DIR}} go install github.com/fullstorydev/grpcurl/cmd/grpcurl@{{.GRPCURL_VERSION}}
        }
    status:
      - test -x {{.GRPCURL}}

# =============================================================================
# ФОРМАТИРОВАНИЕ И ЛИНТИНГ КОДА
# =============================================================================

  format:
    desc: "Форматирует весь проект gofumpt + gci, исключая mocks"
    summary: |
      Форматирует все Go-файлы проекта с использованием gofumpt для стандартизации кода
      и gci для сортировки импортов, исключая файлы в директориях mocks.
      
      Использует инструменты:
        - gofumpt: для стандартизации форматирования
        - gci: для сортировки импортов по стандартным группам
    deps: [ install-formatters ]
    cmds:
      - |
        echo "🧼 Форматируем через gofumpt ..."
        
        for module in {{.MODULES}}; do
          if [ -d "$module" ]; then
            echo "🧼 Форматируем $module"
            find $module -type f -name '*.go' ! -path '*/mocks/*' -exec {{.GOFUMPT}} -extra -w {} +
          fi
        done
      - |
        echo "🎯 Сортируем импорты через gci ..."
        
        for module in {{.MODULES}}; do
          if [ -d "$module" ]; then
            echo "🎯 Сортируем импорты в $module"
            find $module -type f -name '*.go' ! -path '*/mocks/*' -exec {{.GCI}} write -s standard -s default -s "prefix(github.com/Alexander-Mandzhiev/school_schedule)" {} +
          fi
        done

  lint:
    desc: "Запускает golangci-lint для всех модулей"
    summary: |
      Запускает линтер golangci-lint для всех модулей проекта.
      Линтер проверяет код на соответствие стандартам качества и лучшим практикам.
      Проверка включает проверку безопасности через gosec (встроенный в golangci-lint).
      
      Зависимости:
        - install-golangci-lint: автоматически устанавливает линтер
        - format: форматирует код перед проверкой
    deps: [ install-golangci-lint ]
    vars:
      MODULES: '{{.MODULES}}'
      GOLANGCI_LINT: '{{.GOLANGCI_LINT}}'
    cmds:
      - |
        set -e
        ERR=0
        echo "🔍 Линтим все модули ..."
        for mod in {{.MODULES}}; do
          if [ -d "$mod" ]; then
            echo "🔍 Линтим $mod module"
            {{.GOLANGCI_LINT}} run $mod/... --config=.golangci.yml || ERR=1
          fi
        done
        exit $ERR

# =============================================================================
# ГЕНЕРАЦИЯ КОДА (PROTOBUF И OPENAPI)
# =============================================================================

  proto:install-plugins:
    desc: "Устанавливает protoc плагины в каталог bin"
    cmds:
      - |
        [ -f {{.PROTOC_GEN_GO}} ] || {
          echo '📦 Installing protoc-gen-go...'
          GOBIN={{.BIN_DIR}} go install google.golang.org/protobuf/cmd/protoc-gen-go@{{.PROTOC_GEN_GO_VERSION}}
        }
        [ -f {{.PROTOC_GEN_GO_GRPC}} ] || {
          echo '📦 Installing protoc-gen-go-grpc...'
          GOBIN={{.BIN_DIR}} go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@{{.PROTOC_GEN_GO_GRPC_VERSION}}
        }
        [ -f {{.PROTOC_GEN_GRPC_GATEWAY}} ] || {
          echo '📦 Installing protoc-gen-grpc-gateway...'
          GOBIN={{.BIN_DIR}} go install github.com/grpc-ecosystem/grpc-gateway/v2/protoc-gen-grpc-gateway@{{.PROTOC_GEN_GRPC_GATEWAY_VERSION}}
        }
        [ -f {{.PROTOC_GEN_VALIDATE}} ] || {
          echo '📦 Installing protoc-gen-validate...'
          GOBIN={{.BIN_DIR}} go install github.com/envoyproxy/protoc-gen-validate@{{.PROTOC_GEN_VALIDATE_VERSION}}
        }
        [ -f {{.PROTOC_GEN_OPENAPIV2}} ] || {
          echo '📦 Installing protoc-gen-openapiv2...'
          GOBIN={{.BIN_DIR}} go install github.com/grpc-ecosystem/grpc-gateway/v2/protoc-gen-openapiv2@{{.PROTOC_GEN_OPENAPIV2_VERSION}}
        }

  proto:update-deps:
    deps: [ install-buf ]
    desc: Обновляет зависимости protobuf из удаленных репозиториев (googleapis и т.д.)
    dir: shared/proto
    cmds:
      - |
        echo "🔄 Обновляем зависимости buf..."
        {{.BUF}} dep update

  proto:lint:
    deps: [ install-buf, proto:install-plugins ]
    desc: Проверка .proto-файлов на соответствие стилю
    dir: shared/proto
    cmds:
      - '{{.BUF}} lint'

  proto:gen:
    deps: [ install-buf, proto:install-plugins, proto:lint ]
    desc: Генерация Go-кода из .proto
    dir: shared/proto
    cmds:
      - '{{.BUF}} generate --template buf.gen.yaml'

  proto:build:common:
    desc: Сборка proto файлов Common пакета
    deps: [ install-buf, proto:install-plugins ]
    dir: ./shared/proto
    cmds:
      - 'mkdir -p ../pkg/proto/common/v1'
      - '{{.BUF}} build --path common --as-file-descriptor-set --output "../pkg/proto/common/v1/common_descriptor.pb"'

  proto:build:auth:
    desc: Сборка proto файлов Auth сервиса
    deps: [ install-buf, proto:install-plugins, proto:build:common ]
    dir: ./shared/proto
    cmds:
      - 'mkdir -p ../pkg/proto/auth/v1'
      - '{{.BUF}} build --path auth --path common --as-file-descriptor-set --output "../pkg/proto/auth/v1/auth_descriptor.pb"'

  proto:build:user:
    desc: Сборка proto файлов User сервиса
    deps: [ install-buf, proto:install-plugins, proto:build:common ]
    dir: ./shared/proto
    cmds:
      - 'mkdir -p ../pkg/proto/user/v1'
      - '{{.BUF}} build --path user --path common --as-file-descriptor-set --output "../pkg/proto/user/v1/user_descriptor.pb"'

  proto:build:role:
    desc: Сборка proto файлов Role сервиса
    deps: [ install-buf, proto:install-plugins, proto:build:common ]
    dir: ./shared/proto
    cmds:
      - 'mkdir -p ../pkg/proto/role/v1'
      - '{{.BUF}} build --path role --path common --as-file-descriptor-set --output "../pkg/proto/role/v1/role_descriptor.pb"'

  proto:build:permission:
    desc: Сборка proto файлов Permission сервиса
    deps: [ install-buf, proto:install-plugins, proto:build:common ]
    dir: ./shared/proto
    cmds:
      - 'mkdir -p ../pkg/proto/permission/v1'
      - '{{.BUF}} build --path permission --path common --as-file-descriptor-set --output "../pkg/proto/permission/v1/permission_descriptor.pb"'

  proto:build:user_role:
    desc: Сборка proto файлов UserRole сервиса
    deps: [ install-buf, proto:install-plugins, proto:build:common ]
    dir: ./shared/proto
    cmds:
      - 'mkdir -p ../pkg/proto/user_role/v1'
      - '{{.BUF}} build --path user_role --path common --as-file-descriptor-set --output "../pkg/proto/user_role/v1/user_role_descriptor.pb"'

  proto:build:microservices:
    desc: Сборка объединенного дескриптора для всех микросервисов (для Envoy)
    deps: [ install-buf, proto:install-plugins ]
    dir: ./shared/proto
    cmds:
      - 'mkdir -p ../../deploy/compose/envoy'
      - '{{.BUF}} build --as-file-descriptor-set --output "../../deploy/compose/envoy/microservices_descriptor.pb"'

  proto:build:all:
    desc: Сборка всех proto дескрипторов
    deps: [ proto:build:common, proto:build:auth, proto:build:user, proto:build:role, proto:build:permission, proto:build:user_role, proto:build:microservices ]

  gen:
    desc: "Генерация всех proto файлов"
    cmds:
      - task: proto:gen

  mockery:gen:
    desc: "Генерирует моки интерфейсов с помощью mockery"
    deps: [ mockery:install ]
    cmds:
      - |
        echo '🧪 Генерация моков...'
        {{.MOCKERY}}

# =============================================================================
# ЗАВИСИМОСТИ И УПРАВЛЕНИЕ МОДУЛЯМИ
# =============================================================================

  deps:update:
    desc: "Обновление зависимостей в go.mod во всех модулях"
    cmds:
      - |
        echo "🔄 Обновление зависимостей в go.mod во всех модулях"
        for mod in {{.MODULES}}; do
          if [ -d "$mod" ]; then
            echo "🔄 Обновление зависимостей в $mod"
            (cd "$mod" && go mod tidy -compat=1.24) || exit 1
          fi
        done

# =============================================================================
# ТЕСТИРОВАНИЕ И ПОКРЫТИЕ КОДА
# =============================================================================

  test-coverage:
    desc: "Тесты с покрытием бизнес-логики (service/repository), отчёт по каждому модулю + общий"
    cmds:
      - |
        echo "🧪 Запускаем расчёт покрытия..."
        rm -rf {{.COVERAGE_DIR}}
        mkdir -p {{.COVERAGE_DIR}}
        
        ERR=0
        for mod in {{.MODULES}}; do
          echo "📦 Обрабатываем модуль: $mod"
        
          TARGET_PKGS=$(go list ./$mod/... \
            | grep -E '/(internal/(service|repository|api))' \
            | grep -vE '/(mocks|testdata|pkg|proto|pb|cmd)' \
            | paste -sd "," -)
        
          if [ -z "$TARGET_PKGS" ]; then
            echo "⚠️  Нет подходящих пакетов в $mod"
            continue
          fi
        
          go test -coverpkg="$TARGET_PKGS" \
            -coverprofile={{.COVERAGE_DIR}}/$mod.out \
            -covermode=atomic \
            $(echo "$TARGET_PKGS" | tr "," " ") || ERR=1
        done
        
        if [ $ERR -ne 0 ]; then
          echo "❌ Ошибки во время тестов"
          exit $ERR
        fi
        
        echo
        echo "📊 Покрытие по каждому модулю:"
        for mod in {{.MODULES}}; do
          OUTFILE="{{.COVERAGE_DIR}}/$mod.out"
          if [ -f "$OUTFILE" ]; then
            printf " • %s: " "$mod"
            go tool cover -func="$OUTFILE" | tail -n1
          fi
        done
        
        echo
        echo "📦 Склеиваем все покрытия..."
        {
          echo "mode: atomic"
          find {{.COVERAGE_DIR}} -type f -name '*.out' ! -name '{{.COVERAGE_FILE}}' \
            -exec grep -h -v "^mode:" {} +
        } > {{.COVERAGE_DIR}}/{{.COVERAGE_FILE}}
        
        echo
        echo "🧾 Общее покрытие по всем модулям:"
        go tool cover -func={{.COVERAGE_DIR}}/{{.COVERAGE_FILE}} | tail -n1

  coverage:html:
    desc: "Генерирует HTML-отчёт покрытия и открывает его в браузере"
    deps: [ test-coverage ]
    cmds:
      - |
        OUTPUT={{.COVERAGE_DIR}}/coverage.html
        echo "🌐 Генерируем HTML-отчёт..."
        go tool cover -html={{.COVERAGE_DIR}}/{{.COVERAGE_FILE}} -o $OUTPUT

        echo "🚀 Открываем $OUTPUT"
        if command -v open &> /dev/null; then
          open $OUTPUT  # macOS
        elif command -v xdg-open &> /dev/null; then
          xdg-open $OUTPUT  # Linux
        else
          echo "📂 Отчёт сохранён в $OUTPUT (открой вручную)"
        fi

# =============================================================================
# ТЕСТИРОВАНИЕ gRPC
# =============================================================================

  grpc:test:list:
    desc: "Тестирует получение списка через gRPC"
    deps: [ grpcurl:install ]
    cmds:
      - echo "📋 Получаем список через gRPC..."
      - |
        {{.GRPCURL}} -plaintext -d '{}' {{.GRPC_SERVER_ADDR}} list

  grpc:test:get:
    desc: "Тестирует получение по ID через gRPC"
    deps: [ grpcurl:install ]
    cmds:
      - echo "🔍 Получаем по ID через gRPC... (замените ID на реальный)"
      - |
        {{.GRPCURL}} -plaintext -d '{
          "id": "REPLACE_WITH_REAL_ID"
        }' {{.GRPC_SERVER_ADDR}} get

# =============================================================================
# ОЧИСТКА
# =============================================================================

  clean:
    desc: "Очистить сгенерированные файлы"
    cmds:
      - echo "[task] 🧹 Очищаем сгенерированные файлы"
      - find . -name "*.pb.go" -delete
      - find . -name "*.pb.gw.go" -delete
      - echo "✅ Очистка завершена"


# =============================================================================
# DOCKER COMPOSE - УПРАВЛЕНИЕ СЕТЬЮ
# =============================================================================

  up-network:
    desc: "Создать сеть school_net"
    cmds:
      - echo "[task] 🌐 Создаем сеть school_net"
      - docker network create school_net || echo "Сеть уже существует"

  down-network:
    desc: "Удалить сеть school_net"
    cmds:
      - echo "[task] 🗑️ Удаляем сеть school_net"
      - docker network rm school_net || echo "Сеть не существует или используется"

# =============================================================================
# DOCKER COMPOSE - УПРАВЛЕНИЕ СЕРВИСАМИ
# =============================================================================


  # Eventbus сервисы (Kafka)
  up-eventbus:
    desc: "Поднять eventbus контейнеры"
    dir: deploy/compose/eventbus
    cmds:
      - echo "[task] 🔄 Поднимаем eventbus контейнеры"
      - docker compose -p school_schedule_eventbus up --build --detach

  down-eventbus:
    desc: "Остановить и удалить eventbus контейнеры"
    dir: deploy/compose/eventbus
    cmds:
      - echo "[task] 🛑 Останавливаем eventbus контейнеры"
      - docker compose -p school_schedule_eventbus down


  # Tracing сервисы (Jaeger)
  up-tracing:
    desc: "Поднять tracing контейнеры (без OTEL)"
    dir: deploy/compose/tracing
    cmds:
      - echo "[task] 🔍 Поднимаем tracing контейнеры"
      - docker compose -p school_schedule_tracing up --build --detach


  down-tracing:
    desc: "Остановить и удалить tracing контейнеры"
    dir: deploy/compose/tracing
    cmds:
      - echo "[task] 🛑 Останавливаем tracing контейнеры"
      - docker compose -p school_schedule_tracing down

  # Logs сервисы (ELK)
  up-logs:
    desc: "Поднять logs контейнеры (без OTEL)"
    dir: deploy/compose/logs
    cmds:
      - echo "[task] 📝 Поднимаем logs контейнеры"
      - docker compose -p school_schedule_logs up --build --detach


  down-logs:
    desc: "Остановить и удалить logs контейнеры"
    dir: deploy/compose/logs
    cmds:
      - echo "[task] 🛑 Останавливаем logs контейнеры"
      - docker compose -p school_schedule_logs down

  # IAM сервис
  up-iam:
    desc: "Поднять iam сервис и все его зависимости"
    dir: deploy/compose/iam
    cmds: 
      - task: up-network
      - echo "[task] 🔐 Поднимаем iam сервис с зависимостями"
      - docker compose -p school_schedule_iam up --build --detach

  down-iam:
    desc: "Остановить и удалить iam сервис и все его зависимости"
    dir: deploy/compose/iam
    cmds:
      - echo "[task] 🛑 Останавливаем iam сервис с зависимостями"
      - docker compose -p school_schedule_iam down --volumes

  # RBAC сервис
  up-rbac:
    desc: "Поднять rbac сервис и все его зависимости"
    dir: deploy/compose/rbac
    cmds: 
      - task: up-network
      - echo "[task] 🔒 Поднимаем rbac сервис с зависимостями"
      - docker compose -p school_schedule_rbac up --build --detach

  down-rbac:
    desc: "Остановить и удалить rbac сервис и все его зависимости"
    dir: deploy/compose/rbac
    cmds:
      - echo "[task] 🛑 Останавливаем rbac сервис с зависимостями"
      - docker compose -p school_schedule_rbac down --volumes

  # Metrics сервисы (Prometheus + Grafana)
  up-metrics:
    desc: "Поднять metrics контейнеры (без OTEL)"
    dir: deploy/compose/metrics
    cmds:
      - echo "[task] 📊 Поднимаем metrics контейнеры"
      - docker compose -p school_schedule_metrics up --build --detach


  down-metrics:
    desc: "Остановить и удалить metrics контейнеры"
    dir: deploy/compose/metrics
    cmds:
      - echo "[task] 🛑 Останавливаем metrics контейнеры"
      - docker compose -p school_schedule_metrics down --volumes

  # OTEL Collector
  up-otel:
    desc: "Поднять единый OTEL коллектор"
    dir: deploy/compose/otel
    cmds:
      - echo "[task] 📡 Поднимаем единый OTEL коллектор"
      - docker compose -p school_schedule_otel up --build --detach

  down-otel:
    desc: "Остановить и удалить OTEL коллектор"
    dir: deploy/compose/otel
    cmds:
      - echo "[task] 🛑 Останавливаем OTEL коллектор"
      - docker compose -p school_schedule_otel down

  up-cache:
    desc: "Поднять cache сервис (Redis) с зависимостями"
    dir: deploy/compose/cache
    cmds:
      - task: up-network
      - echo "[task] 🚀 Поднимаем cache сервис"
      - docker compose -p school_schedule_cache up --build --detach
  down-cache:
    desc: "Остановить и удалить cache сервис с зависимостями"
    dir: deploy/compose/cache
    cmds:
      - echo "[task] 🛑 Останавливаем cache сервис"
      - docker compose -p school_schedule_cache down --volumes
      
  up-envoy:
    desc: "Поднять Envoy API Gateway"
    dir: deploy/compose/envoy
    cmds:
      - task: up-network
      - task: proto:build:microservices
      - echo "[task] 🌐 Поднимаем Envoy API Gateway"
      - docker compose -p school_schedule_envoy up --build --detach

  down-envoy:
    desc: "Остановить и удалить Envoy API Gateway"
    dir: deploy/compose/envoy
    cmds:
      - echo "[task] 🛑 Останавливаем Envoy API Gateway"
      - docker compose -p school_schedule_envoy down --volumes

# =============================================================================
# DOCKER COMPOSE - УПРАВЛЕНИЕ ВСЕМИ СЕРВИСАМИ
# =============================================================================

  up-all:
    desc: "Поднять все сервисы с единым OTEL коллектором"
    cmds:
      - task: up-network
      - task: up-cache
      - task: up-eventbus
      - task: up-tracing
      - task: up-logs
      - task: up-metrics
      - task: up-otel
      - task: up-iam
      - task: up-rbac
      - task: up-envoy
      - echo 'Все сервисы подняты с domain-specific OTEL!'
      - echo 'Traces HAProxy stats - http://localhost:8408/stats'
      - echo 'API Gateway - http://localhost:8080'

  down-all:
    desc: "Остановить и удалить все сервисы по очереди вместе с зависимостями"
    cmds:
      - task: down-envoy
      - task: down-rbac
      - task: down-iam
      - task: down-otel
      - task: down-metrics
      - task: down-logs
      - task: down-tracing
      - task: down-cache
      - task: down-eventbus
      - task: down-network
      - echo "✅ Все сервисы остановлены!"

  restart-all:
    desc: "Перезапустить все сервисы"
    cmds:
      - task: down-all
      - task: up-all

# =============================================================================
# DOCKER COMPOSE - МОНИТОРИНГ И ЛОГИ
# =============================================================================

  status:
    desc: "Показать статус всех контейнеров"
    cmds:
      - echo "[task] 📊 Статус всех контейнеров"
      - docker ps

  logs:
    desc: "Показать логи всех сервисов"
    cmds:
      - echo "[task] 📋 Логи всех сервисов"
      - echo "--- Eventbus logs ---"
      - docker compose -p school_schedule_eventbus -f deploy/compose/eventbus/docker-compose.yaml logs --tail=20
      - echo "--- IAM logs ---"
      - docker compose -p school_schedule_iam -f deploy/compose/iam/docker-compose.yml logs --tail=20
      - echo "--- Metrics logs ---"
      - docker compose -p school_schedule_metrics -f deploy/compose/metrics/docker-compose.yaml logs --tail=20
      - echo "--- Logs logs ---"
      - docker compose -p school_schedule_logs -f deploy/compose/logs/docker-compose.yml logs --tail=20
      - echo "--- Tracing logs ---"
      - docker compose -p school_schedule_tracing -f deploy/compose/tracing/docker-compose.yml logs --tail=20
      - echo "--- OTEL logs ---"
      - docker compose -p school_schedule_otel -f deploy/compose/otel/docker-compose.yml logs --tail=20


  logs-eventbus:
    desc: "Логи eventbus сервисов"
    dir: deploy/compose/eventbus
    cmds:
      - docker compose -p school_schedule_eventbus logs --tail=50

  logs-iam:
    desc: "Логи iam сервиса"
    dir: deploy/compose/iam
    cmds:
      - docker compose -p school_schedule_iam logs --tail=50

  logs-metrics:
    desc: "Логи metrics сервисов"
    dir: deploy/compose/metrics
    cmds:
      - docker compose -p school_schedule_metrics logs --tail=50

  logs-logs:
    desc: "Логи logs сервисов"
    dir: deploy/compose/logs
    cmds:
      - docker compose -p school_schedule_logs logs --tail=50

  logs-tracing:
    desc: "Логи tracing сервисов"
    dir: deploy/compose/tracing
    cmds:
      - docker compose -p school_schedule_tracing logs --tail=50

  logs-otel:
    desc: "Логи otel сервисов"
    dir: deploy/compose/otel
    cmds:
      - docker compose -p school_schedule_otel logs --tail=50

# =============================================================================
# DOCKER COMPOSE - ОЧИСТКА И УПРАВЛЕНИЕ РЕСУРСАМИ
# =============================================================================

  clean:
    desc: "Очистить все контейнеры, образы и volumes"
    cmds:
      - echo "[task] 🧹 Очищаем все Docker ресурсы"
      - docker system prune -a --volumes -f
      - echo "✅ Очистка завершена"

# =============================================================================
# API ТЕСТИРОВАНИЕ ЧЕРЕЗ ENVOY
# =============================================================================

  api:test:health:
    desc: "Проверить health check через Envoy"
    cmds:
      - echo "[task] 🏥 Проверяем health check"
      - curl -sS http://localhost:8080/healthz || echo "❌ Health check failed"

  api:test:register:
    desc: "Тестировать регистрацию пользователя через Envoy"
    cmds:
      - echo "[task] 👤 Регистрируем пользователя"
      - |
        curl -sS -X POST http://localhost:8080/api/v1/users/register \
          -H 'content-type: application/json' \
          -d '{"info":{"login":"testuser","email":"test@example.com"},"password":"secret123"}' \
          | jq . || echo "❌ Registration failed"

  api:test:login:
    desc: "Тестировать логин пользователя через Envoy"
    cmds:
      - echo "[task] 🔐 Логинимся пользователем"
      - |
        curl -sS -X POST http://localhost:8080/api/v1/auth/login \
          -H 'content-type: application/json' \
          -d '{"login":"testuser","password":"secret123"}' \
          | jq . || echo "❌ Login failed"

  api:test:whoami:
    desc: "Автоматически получить session_id из логина и вызвать whoami"
    cmds:
      - |
        echo "[task] 🔐 Сначала логинимся и получаем session_id"
        SESSION_ID=$(curl -sS -X POST http://localhost:8080/api/v1/auth/login \
          -H 'content-type: application/json' \
          -d '{"login":"testuser","password":"secret123"}' \
          | jq -r '.session_id' 2>/dev/null)
        if [ "$SESSION_ID" = "null" ] || [ -z "$SESSION_ID" ]; then
          echo "❌ Не удалось получить session_id из логина"
          exit 1
        fi
        echo "✅ Получен session_id: $SESSION_ID"
        echo "[task] 👤 Теперь получаем информацию о пользователе"
        curl -sS -X GET http://localhost:8080/api/v1/auth/whoami \
          -H "Cookie: X-Session-Uuid=$SESSION_ID" \
          | jq . || echo "❌ Whoami failed"

  api:test:logout:
    desc: "Автоматически получить session_id из логина и вызвать logout"
    cmds:
      - echo "[task] 🔐 Сначала логинимся и получаем session_id"
      - |
        SESSION_ID=$(curl -sS -X POST http://localhost:8080/api/v1/auth/login \
          -H 'content-type: application/json' \
          -d '{"login":"testuser","password":"secret123"}' \
          | jq -r '.session_id' 2>/dev/null)
      - |
        if [ "$SESSION_ID" = "null" ] || [ -z "$SESSION_ID" ]; then
          echo "❌ Не удалось получить session_id из логина"
          exit 1
        fi
      - |
        echo "✅ Получен session_id: $SESSION_ID"
      - echo "[task] 🚪 Теперь выходим из системы"
      - |
        curl -sS -X POST http://localhost:8080/api/v1/auth/logout \
          -H 'content-type: application/json' \
          -H "sid: $SESSION_ID" \
          -d '{"session_id":"'$SESSION_ID'"}' \
          | jq . || echo "❌ Logout failed"

  api:test-full:
    desc: "Полный тест всех RBAC операций: регистрация, логин, whoami, CRUD ролей, назначение/отзыв ролей, logout"
    cmds:
      - echo "[task] 🔄 Запускаем полный тест всех RBAC операций"
      - |
        echo "=========================================="
        echo "1️⃣ Регистрация нового пользователя"
        USER_LOGIN="rbactest$(date +%s)"
        USER_EMAIL="rbactest$(date +%s)@example.com"
        echo "📝 Регистрируем пользователя: $USER_LOGIN"
        curl -sS -X POST http://localhost:8080/api/v1/users/register \
          -H 'content-type: application/json' \
          -d '{"info":{"login":"'$USER_LOGIN'","email":"'$USER_EMAIL'"},"password":"secret123"}' \
          | jq . || echo "❌ Registration failed"
        
        echo "=========================================="
        echo "2️⃣ Логин пользователя"
        SESSION_ID=$(curl -sS -X POST http://localhost:8080/api/v1/auth/login \
          -H 'content-type: application/json' \
          -d '{"login":"'$USER_LOGIN'","password":"secret123"}' \
          | jq -r '.session_id')
        echo "📝 Session ID: $SESSION_ID"
        if [ "$SESSION_ID" = "null" ] || [ -z "$SESSION_ID" ]; then
          echo "❌ Login failed"
          exit 1
        fi
        
        echo "=========================================="
        echo "3️⃣ Получение информации о пользователе (whoami)"
        curl -sS -X GET http://localhost:8080/api/v1/auth/whoami \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Whoami failed"
        
        echo "=========================================="
        echo "4️⃣ Получение списка ролей (до создания)"
        curl -sS -X GET http://localhost:8080/api/v1/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Roles list failed"
        
        echo "=========================================="
        echo "5️⃣ Создание новой роли"
        ROLE_NAME="TestRole$(date +%s)"
        echo "📝 Создаем роль: $ROLE_NAME"
        ROLE_ID=$(curl -sS -X POST http://localhost:8080/api/v1/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          -d '{"name":"'$ROLE_NAME'","description":"Test role for RBAC testing"}' \
          | jq -r '.role_id')
        echo "📝 Created Role ID: $ROLE_ID"
        if [ "$ROLE_ID" = "null" ] || [ -z "$ROLE_ID" ]; then
          echo "❌ Role creation failed"
          exit 1
        fi
        
        echo "=========================================="
        echo "6️⃣ Получение созданной роли по ID"
        curl -sS -X GET http://localhost:8080/api/v1/roles/$ROLE_ID \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Get role failed"
        
        echo "=========================================="
        echo "7️⃣ Обновление роли"
        UPDATED_NAME="Updated$ROLE_NAME"
        echo "📝 Обновляем роль на: $UPDATED_NAME"
        curl -sS -X PUT http://localhost:8080/api/v1/roles/$ROLE_ID \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          -d '{"name":"'$UPDATED_NAME'","description":"Updated test role"}' \
          | jq . || echo "❌ Role update failed"
        
        echo "=========================================="
        echo "8️⃣ Получение списка ролей (после создания)"
        curl -sS -X GET http://localhost:8080/api/v1/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Roles list failed"
        
        echo "=========================================="
        echo "9️⃣ Получение списка прав"
        curl -sS -X GET http://localhost:8080/api/v1/permissions \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Permissions list failed"
        
        echo "=========================================="
        echo "🔟 Получение ролей пользователя (до назначения)"
        USER_ID=$(curl -sS -X GET http://localhost:8080/api/v1/auth/whoami \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq -r '.info.user.id')
        echo "📝 User ID: $USER_ID"
        curl -sS -X GET http://localhost:8080/api/v1/users/$USER_ID/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Get user roles failed"
        
        echo "=========================================="
        echo "1️⃣1️⃣ Назначение роли пользователю"
        echo "📝 Назначаем роль $ROLE_ID пользователю $USER_ID"
        curl -sS -X POST http://localhost:8080/api/v1/users/$USER_ID/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          -d '{"role_id":"'$ROLE_ID'"}' \
          | jq . || echo "❌ Assign role failed"
        
        echo "=========================================="
        echo "1️⃣2️⃣ Получение ролей пользователя (после назначения)"
        curl -sS -X GET http://localhost:8080/api/v1/users/$USER_ID/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Get user roles failed"
        
        echo "=========================================="
        echo "1️⃣3️⃣ Получение пользователей роли"
        curl -sS -X GET http://localhost:8080/api/v1/roles/$ROLE_ID/users \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Get role users failed"
        
        echo "=========================================="
        echo "1️⃣4️⃣ Отзыв роли у пользователя"
        echo "📝 Отзываем роль $ROLE_ID у пользователя $USER_ID"
        curl -sS -X DELETE http://localhost:8080/api/v1/users/$USER_ID/roles/$ROLE_ID \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Revoke role failed"
        
        echo "=========================================="
        echo "1️⃣5️⃣ Получение ролей пользователя (после отзыва)"
        curl -sS -X GET http://localhost:8080/api/v1/users/$USER_ID/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Get user roles failed"
        
        echo "=========================================="
        echo "1️⃣6️⃣ Удаление роли"
        echo "📝 Удаляем роль $ROLE_ID"
        curl -sS -X DELETE http://localhost:8080/api/v1/roles/$ROLE_ID \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Delete role failed"
        
        echo "=========================================="
        echo "1️⃣7️⃣ Получение списка ролей (после удаления)"
        curl -sS -X GET http://localhost:8080/api/v1/roles \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          | jq . || echo "❌ Roles list failed"
        
        echo "=========================================="
        echo "1️⃣8️⃣ Выход из системы"
        curl -sS -X POST http://localhost:8080/api/v1/auth/logout \
          -H 'content-type: application/json' \
          -H "Cookie: X-Session-Id=$SESSION_ID" \
          -d '{}' \
          | jq . || echo "❌ Logout failed"
        
        echo "=========================================="
        echo "✅ Полный тест всех RBAC операций завершен!"
        echo "📊 Тестированы все операции:"
        echo "   • Регистрация и аутентификация"
        echo "   • CRUD операции с ролями"
        echo "   • Назначение и отзыв ролей пользователям"
        echo "   • Получение списков ролей и прав"
        echo "   • Выход из системы"
