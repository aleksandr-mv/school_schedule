apiVersion: v1
kind: ConfigMap
metadata:
  name: iam-config
  namespace: school-schedule-services
data:
  development.yaml: |
    server:
      http:
        host: "0.0.0.0"
        port: 8080
      grpc:
        host: "0.0.0.0"
        port: 50051
    
    database:
      write:
        host: "postgres-iam"
        port: 5432
        database: "iam"
        user: "postgres"
        password: "iam_password"
        ssl_mode: "disable"
      read:
        host: "postgres-iam"
        port: 5432
        database: "iam"
        user: "postgres"
        password: "iam_password"
        ssl_mode: "disable"
    
    redis:
      cluster:
        nodes: ["redis-cluster:6379"]
        password: ""
    
    kafka:
      brokers: ["kafka:9092"]
      topics:
        user_created: "user-created"
    
    services:
      rbac:
        host: "rbac-service"
        port: 50052
        timeout: "30s"
    
    logger:
      level: "info"
      format: "json"
      otlp:
        enable: true
        endpoint: "http://otel-collector:4317"
    
    metric:
      enable: true
      endpoint: "http://otel-collector:4317"
    
    tracing:
      enable: true
      endpoint: "http://otel-collector:4317"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iam-service
  namespace: school-schedule-services
spec:
  replicas: 3
  selector:
    matchLabels:
      app: iam-service
  template:
    metadata:
      labels:
        app: iam-service
    spec:
      containers:
      - name: iam-service
        image: school-schedule/iam:latest
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 50051
          name: grpc
        env:
        - name: CONFIG_PATH
          value: "/app/config/development.yaml"
        volumeMounts:
        - name: iam-config
          mountPath: /app/config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: iam-config
        configMap:
          name: iam-config
---
apiVersion: v1
kind: Service
metadata:
  name: iam-service
  namespace: school-schedule-services
spec:
  selector:
    app: iam-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 50051
      targetPort: 50051
  type: ClusterIP
